# NOTES (append only, never edit others' entries)

- [18:05] hudson/agent: Project = real Doom (doomgeneric + Freedoom) inside an Ink Claude Code 2.1 lookalike. I own `engine/`, teammate owns `ui/`. Protocol is in `CONTRACT.md`. First engine push coming within ~20 min; until then UI can develop against the `F`/`S`/`L` line format.
- [18:08] hudson/agent: engine pushed and working. `make -C engine && node engine/dev/play.mjs` plays Freedoom E1M1 raw. Protocol in CONTRACT.md is live; smoke test: `printf "s 80 40\n" | timeout 3 engine/build/doom-term -iwad engine/wads/freedoom1.wad -warp 1 | head -c 2000`.
- [18:18] hudson/agent: engine additions (all additive, see CONTRACT.md): `m blocks|ascii|mono|braille` render modes, `c <text>` types cheats (c iddqd), `r <ms>` release tuning, S line now has weapon/x/y/angle/sector for cwd and model-line gags.
- [18:21] hudson/agent: added `engine/dev/fake-engine.mjs`, a Doom-free stand-in for `doom-term` with the same protocol, for UI dev/testing. Also `f <fps>` cap command.
- [18:30] hudson/agent: added `narrator/` (mine): `node narrator/wrap.mjs` proxies the engine and adds `N <text>` Claude narration lines (claude-fable-5-1, canned fallback without a key). Engine now emits `E dead|respawn|level-done|level-start|kill N|hurt N` events for the crash gag; fake engine supports `c die`. UIs must ignore unknown line prefixes.
- [18:35] hudson/agent: added `./demo.sh` (root, shared): builds engine, runs `ui/` via `bun run start`/`npm start` if `ui/package.json` exists, else raw player with narration. Teammate: make sure `ui/package.json` has a `start` script.
- [18:34] matt/agent: `ui/` scaffold pushed (Ink + tsx). `cd ui && npm i && npm run mock` runs against fake-engine.mjs; `npm start` spawns the real engine (via `wsl.exe` on Windows). Uses only s/k/m/q and F/S/L, no contract changes. Header uses `weapon` + `sector` from S.
- [18:40] hudson/agent: reviewed ui/ against CONTRACT.md: clean, no issues. Full stack verified here (Linux): `./demo.sh` -> UI + narrator + real Doom, real Claude N lines in the spinner. demo.sh now uses npm and installs narrator deps first. Extra args pass through: `./demo.sh ui --exit-after 8000`.
- [18:42] hudson (via agent) -> matt/agent, UI feedback from a live run (screenshot at 18:41), all in ui/ so leaving to you: (1) token count shows twice: spinner line "↑ 9.7k tokens" and status line "↓ 9.7k tokens"; please drop one. (2) Tool log rows are not cleared to end of line, so a shorter line leaves stale text: saw "L  engine readye/dev/fake-engine.mjs)"; pad rows to width or emit \x1b[K. (3) Long narration wraps the spinner line onto a second row and pushes the status line; consider truncating the N text to cols minus the suffix width. Everything else looks great.
- [18:53] matt/agent: ui fixes for 18:42 feedback pushed: tokens only in status line, all chrome lines truncate to width (no wraps -> no stale rows), narration trimmed to fit before the (esc to interrupt) suffix.
- [19:00] hudson/agent: added `mcp/` (mine): MCP server exposing Doom as tools so real Claude Code plays it turn-based (`doom_start/look/press/type/stop`, prompt `/doom:play`). Registered locally only (`claude mcp add --scope local`), no .mcp.json in the repo, no engine/ui changes. See mcp/README.md.
